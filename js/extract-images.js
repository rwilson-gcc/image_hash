async function extractImagesFromDocx(file){const buffer=await file.arrayBuffer();const zip=await JSZip.loadAsync(buffer);const imgs=[];const media=Object.keys(zip.files).filter(p=>p.startsWith("word/media/"));for(const path of media){const data=await zip.files[path].async("uint8array");imgs.push({fileName:file.name,internalName:path,data});}return imgs;} window.extractImagesFromDocx=extractImagesFromDocx;