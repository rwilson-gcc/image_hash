function renderOutput(shaGroups){let results=document.getElementById("results");results.innerHTML="";results.innerHTML+=`<button class="accordion">SHA‑256 Exact Matches <span class="icon">▶</span></button><div class="panel" id="shaPanel"></div>`;let sp=document.getElementById("shaPanel");Object.entries(shaGroups).forEach(([hash,imgs])=>{if(imgs.length<=1)return;let html=`<div class="match-group"><h3>${imgs.length} Exact Matches</h3><code>${hash}</code>`;imgs.forEach(i=>html+=renderCard(i));html+=`</div>`;sp.innerHTML+=html;});results.innerHTML+=`<button class="accordion">Unique Images <span class="icon">▶</span></button><div class="panel" id="uniqPanel"></div>`;let up=document.getElementById("uniqPanel");Object.entries(shaGroups).forEach(([hash,imgs])=>{if(imgs.length===1)up.innerHTML+=renderCard(imgs[0]);});} window.renderOutput=renderOutput;